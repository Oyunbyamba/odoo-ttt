<?xml version="1.0"?>
<odoo>
    <data>

        <!-- hr related job position menu action -->
        <record id="ir_attachment_job_applicants" model="ir.actions.act_window">
            <field name="name">Ажилд горилогчдын хавсралтууд</field>
            <field name="res_model">hr.employee</field>
            <field name="view_mode">tree,form</field>
            <field name="domain">[]</field>
            <field name="context">{}</field>
            <field name="view_id" eval="False"/>
            <field name="help" type="html">
             <p class="o_view_nocontent_smiling_face">
               Add a new employee
             </p><p>
               With just a quick glance on the Odoo employee screen, you
               can easily find all the information you need for each person;
               contact data, job position, availability, etc.
             </p>
            </field>
        </record>

        <record id="view_attachment_tree" model="ir.ui.view">
            <field name="name">ir.attachment.tree</field>
            <field name="model">ir.attachment</field>
            <field name="arch" type="xml">
                <tree string="Employees">
                    <field name="name"/>
                    <field name="res_model"/>
                </tree>
            </field>
        </record>
        
        <menuitem id="hr_applicant_attachments" 
            name="Хавсралтууд"
            parent="hr_recruitment.menu_crm_case_categ0_act_job"
            groups="base.group_user"
            action="ir_attachment_job_applicants"
            sequence="3"
            />

        <menuitem id="hr_recruitment.menu_hr_job_position_config" groups="ubisol_hr_recruitment.hide"/>

    </data>
</odoo>
